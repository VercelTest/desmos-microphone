const FFT_SIZE = 4096; // Tradeoff clearer frequency vs time resolution: Try find a balance         
const STEP_RATIO = 0.5; // smaller equals better time resolution (but this will increase processing time a LOT)
const MAX_PEAKS = 3;    // max amount of peaks to keep per sample
const MIN_MAGNITUDE = 0.02; // threshold to ignore quiet harmonics     
const MAX_FREQUENCY = 15000;    
const PROGRESS_INTERVAL = 5; // how often to send progress updates to the green bar

const FFT_SIZE = 8192
const STEP_RATIO = 0.1;       
const MAX_PEAKS = 5;         
const MIN_MAGNITUDE = 0.01;     
const MAX_FREQUENCY = 18000;   
const PROGRESS_INTERVAL = 50; 